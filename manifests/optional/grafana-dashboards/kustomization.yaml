apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- dashboards-configmaplist.yaml

commonLabels:
  app: grafana
  component: grafana
  grafana_dashboard: "1"

namespace: monitoring