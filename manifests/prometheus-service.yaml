apiVersion: v1
kind: Service
metadata:
  labels:
    prometheus: k8s
  name: prometheus-k8s
  namespace: monitoring
spec:
  ports:
  - name: tenancy
    port: 9092
    targetPort: tenancy
  selector:
    app: prometheus
    prometheus: k8s
  sessionAffinity: ClientIP
